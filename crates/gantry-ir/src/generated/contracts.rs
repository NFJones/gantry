// @generated by `cargo run --locked -p xtask -- generate protocol`.
// Source: protocol/catalogs/ir-contracts-v1.json. Do not edit manually.

/// Closed `ArtifactKind` portable IR artifact vocabulary.
#[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
pub enum ArtifactKind {
    /// The `canonical-ir` value.
    CanonicalIr,
    /// The `generated-schema-object` value.
    GeneratedSchemaObject,
    /// The `package-source-manifest` value.
    PackageSourceManifest,
    /// The `source-map` value.
    SourceMap,
}

impl ArtifactKind {
    /// Returns the exact portable spelling.
    #[must_use]
    pub const fn wire_name(self) -> &'static str {
        match self {
            Self::CanonicalIr => "canonical-ir",
            Self::GeneratedSchemaObject => "generated-schema-object",
            Self::PackageSourceManifest => "package-source-manifest",
            Self::SourceMap => "source-map",
        }
    }
}

/// Closed `AnalysisFactKind` generic analysis fact vocabulary.
#[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
pub enum AnalysisFactKind {
    /// The `concrete-effect` value.
    ConcreteEffect,
    /// The `concrete-instantiation` value.
    ConcreteInstantiation,
    /// The `generic-template` value.
    GenericTemplate,
    /// The `implementation-head` value.
    ImplementationHead,
    /// The `predicate` value.
    Predicate,
    /// The `resolved-call` value.
    ResolvedCall,
    /// The `source-origin-set` value.
    SourceOriginSet,
    /// The `trait-contract` value.
    TraitContract,
}

impl AnalysisFactKind {
    /// Returns the exact portable spelling.
    #[must_use]
    pub const fn wire_name(self) -> &'static str {
        match self {
            Self::ConcreteEffect => "concrete-effect",
            Self::ConcreteInstantiation => "concrete-instantiation",
            Self::GenericTemplate => "generic-template",
            Self::ImplementationHead => "implementation-head",
            Self::Predicate => "predicate",
            Self::ResolvedCall => "resolved-call",
            Self::SourceOriginSet => "source-origin-set",
            Self::TraitContract => "trait-contract",
        }
    }
}

/// Closed `CoreForm` desugared core form vocabulary.
#[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
pub enum CoreForm {
    /// The `aggregate` value.
    Aggregate,
    /// The `assignment` value.
    Assignment,
    /// The `attempt` value.
    Attempt,
    /// The `background-transfer` value.
    BackgroundTransfer,
    /// The `branch` value.
    Branch,
    /// The `call` value.
    Call,
    /// The `cancellation-check` value.
    CancellationCheck,
    /// The `join` value.
    Join,
    /// The `literal` value.
    Literal,
    /// The `loop` value.
    Loop,
    /// The `operation` value.
    Operation,
    /// The `projection` value.
    Projection,
    /// The `return` value.
    Return,
    /// The `sequence` value.
    Sequence,
    /// The `session-scope` value.
    SessionScope,
    /// The `spawn` value.
    Spawn,
    /// The `variable` value.
    Variable,
    /// The `with-scope` value.
    WithScope,
}

impl CoreForm {
    /// Returns the exact portable spelling.
    #[must_use]
    pub const fn wire_name(self) -> &'static str {
        match self {
            Self::Aggregate => "aggregate",
            Self::Assignment => "assignment",
            Self::Attempt => "attempt",
            Self::BackgroundTransfer => "background-transfer",
            Self::Branch => "branch",
            Self::Call => "call",
            Self::CancellationCheck => "cancellation-check",
            Self::Join => "join",
            Self::Literal => "literal",
            Self::Loop => "loop",
            Self::Operation => "operation",
            Self::Projection => "projection",
            Self::Return => "return",
            Self::Sequence => "sequence",
            Self::SessionScope => "session-scope",
            Self::Spawn => "spawn",
            Self::Variable => "variable",
            Self::WithScope => "with-scope",
        }
    }
}

/// Closed `ExecutableFactKind` closed executable fact vocabulary.
#[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
pub enum ExecutableFactKind {
    /// The `closed-callable` value.
    ClosedCallable,
    /// The `closed-type` value.
    ClosedType,
    /// The `direct-call` value.
    DirectCall,
    /// The `operation-site` value.
    OperationSite,
    /// The `task-site` value.
    TaskSite,
}

impl ExecutableFactKind {
    /// Returns the exact portable spelling.
    #[must_use]
    pub const fn wire_name(self) -> &'static str {
        match self {
            Self::ClosedCallable => "closed-callable",
            Self::ClosedType => "closed-type",
            Self::DirectCall => "direct-call",
            Self::OperationSite => "operation-site",
            Self::TaskSite => "task-site",
        }
    }
}

/// Closed `Effect` canonical inferred effect vocabulary.
#[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
pub enum Effect {
    /// The `prompt` value.
    Prompt,
    /// The `decide` value.
    Decide,
    /// The `action(read_only)` value.
    ActionReadOnly,
    /// The `action(idempotent)` value.
    ActionIdempotent,
    /// The `action(non_idempotent)` value.
    ActionNonIdempotent,
    /// The `spawn` value.
    Spawn,
    /// The `join` value.
    Join,
    /// The `background` value.
    Background,
    /// The `session` value.
    Session,
    /// The `attempt` value.
    Attempt,
}

impl Effect {
    /// Returns the exact portable spelling.
    #[must_use]
    pub const fn wire_name(self) -> &'static str {
        match self {
            Self::Prompt => "prompt",
            Self::Decide => "decide",
            Self::ActionReadOnly => "action(read_only)",
            Self::ActionIdempotent => "action(idempotent)",
            Self::ActionNonIdempotent => "action(non_idempotent)",
            Self::Spawn => "spawn",
            Self::Join => "join",
            Self::Background => "background",
            Self::Session => "session",
            Self::Attempt => "attempt",
        }
    }
}

/// Closed `OperationSiteKind` integration-operation site kind vocabulary.
#[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
pub enum OperationSiteKind {
    /// The `prompt` value.
    Prompt,
    /// The `decide` value.
    Decide,
    /// The `action` value.
    Action,
}

impl OperationSiteKind {
    /// Returns the exact portable spelling.
    #[must_use]
    pub const fn wire_name(self) -> &'static str {
        match self {
            Self::Prompt => "prompt",
            Self::Decide => "decide",
            Self::Action => "action",
        }
    }
}

/// Closed `RecoveryClass` action recovery class vocabulary.
#[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
pub enum RecoveryClass {
    /// The `read_only` value.
    ReadOnly,
    /// The `idempotent` value.
    Idempotent,
    /// The `non_idempotent` value.
    NonIdempotent,
}

impl RecoveryClass {
    /// Returns the exact portable spelling.
    #[must_use]
    pub const fn wire_name(self) -> &'static str {
        match self {
            Self::ReadOnly => "read_only",
            Self::Idempotent => "idempotent",
            Self::NonIdempotent => "non_idempotent",
        }
    }
}

/// Closed `TaskControlSiteKind` task-control site kind vocabulary.
#[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
pub enum TaskControlSiteKind {
    /// The `spawn` value.
    Spawn,
    /// The `join` value.
    Join,
    /// The `joinall` value.
    JoinAll,
    /// The `detach` value.
    Detach,
}

impl TaskControlSiteKind {
    /// Returns the exact portable spelling.
    #[must_use]
    pub const fn wire_name(self) -> &'static str {
        match self {
            Self::Spawn => "spawn",
            Self::Join => "join",
            Self::JoinAll => "joinall",
            Self::Detach => "detach",
        }
    }
}

/// Closed `TemplateKind` generic instantiation template kind vocabulary.
#[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
pub enum TemplateKind {
    /// The `declared-type` value.
    DeclaredType,
    /// The `free-workflow` value.
    FreeWorkflow,
    /// The `inherent-method` value.
    InherentMethod,
    /// The `trait-method` value.
    TraitMethod,
}

impl TemplateKind {
    /// Returns the exact portable spelling.
    #[must_use]
    pub const fn wire_name(self) -> &'static str {
        match self {
            Self::DeclaredType => "declared-type",
            Self::FreeWorkflow => "free-workflow",
            Self::InherentMethod => "inherent-method",
            Self::TraitMethod => "trait-method",
        }
    }
}

/// Closed `TypeExpressionKind` generic template type expression vocabulary.
#[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
pub enum TypeExpressionKind {
    /// The `builtin-application` value.
    BuiltinApplication,
    /// The `declared-application` value.
    DeclaredApplication,
    /// The `parameter` value.
    Parameter,
    /// The `primitive` value.
    Primitive,
    /// The `self` value.
    SelfType,
}

impl TypeExpressionKind {
    /// Returns the exact portable spelling.
    #[must_use]
    pub const fn wire_name(self) -> &'static str {
        match self {
            Self::BuiltinApplication => "builtin-application",
            Self::DeclaredApplication => "declared-application",
            Self::Parameter => "parameter",
            Self::Primitive => "primitive",
            Self::SelfType => "self",
        }
    }
}

/// Closed `TypeKind` closed Gantry type kind vocabulary.
#[derive(Clone, Copy, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
pub enum TypeKind {
    /// The `Unit` value.
    Unit,
    /// The `Bool` value.
    Bool,
    /// The `Int` value.
    Int,
    /// The `Float` value.
    Float,
    /// The `String` value.
    String,
    /// The `Declared` value.
    Declared,
    /// The `Option` value.
    Option,
    /// The `Result` value.
    Result,
    /// The `List` value.
    List,
    /// The `Tuple` value.
    Tuple,
    /// The `Decision` value.
    Decision,
    /// The `OperationError` value.
    OperationError,
}

impl TypeKind {
    /// Returns the exact portable spelling.
    #[must_use]
    pub const fn wire_name(self) -> &'static str {
        match self {
            Self::Unit => "Unit",
            Self::Bool => "Bool",
            Self::Int => "Int",
            Self::Float => "Float",
            Self::String => "String",
            Self::Declared => "Declared",
            Self::Option => "Option",
            Self::Result => "Result",
            Self::List => "List",
            Self::Tuple => "Tuple",
            Self::Decision => "Decision",
            Self::OperationError => "OperationError",
        }
    }
}

/// Canonical-IR protocol version.
pub const CANONICAL_IR_VERSION: (u64, u64) = (1, 0);

/// Source-map protocol version.
pub const SOURCE_MAP_VERSION: (u64, u64) = (1, 0);
